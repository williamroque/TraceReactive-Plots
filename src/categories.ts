import type { NodeCategory } from '@tracereactive/types';

export const PlotCategory: NodeCategory = { 
    name: 'Chart (Plots)', 
    accent: 'teal-400', 
    packageId: 'com.tracereactive.plots' 
};
