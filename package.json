{
  "name": "@tracereactive/plots",
  "version": "1.0.0",
  "description": "TraceReactive Plots package",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --platform=browser",
    "build:trpkg": "npm run build && node scripts/build-trpkg.js",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "@tracereactive/types": "file:../types",
    "adm-zip": "^0.5.10",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "jstat": "^1.9.6",
    "mathjs": "^13.0.0",
    "simple-statistics": "^7.12.0"
  }
}
